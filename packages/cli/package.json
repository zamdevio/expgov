{
  "name": "expgov",
  "version": "1.1.0",
  "private": true,
  "type": "module",
  "description": "Workspace-only CLI sources for expgov. The installable npm package is the repository root (@expgov/cli). Keep version aligned with root package.json and @expgov/core on every release.",
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@inquirer/prompts": "^8.3.2",
    "@expgov/core": "workspace:*",
    "chalk": "^5.4.1",
    "commander": "^12.1.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.0"
  }
}
